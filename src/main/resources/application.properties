# Perfil por defecto (desarrollo local)
spring.profiles.active=dev

# Puerto (Railway inyecta PORT)
server.port=${PORT:8080}

# JWT - Clave de al menos 32 caracteres
jwt.secret=miClaveSecretaSuperSeguraParaJWT2024Con32Caracteres!
jwt.expiration=86400000